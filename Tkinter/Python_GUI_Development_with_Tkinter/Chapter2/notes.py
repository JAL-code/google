# Pack, Grid and Place are Tk Geometry Managers

# Handling User Events
# <ButtonPress> handle_buttonpress()
# <Key>         handle_key()
# <Leave>       handle_leave()
# <Motion>      handle_motion()
# <Configure>   handle_configure()

# Event loop

# Configuring Event Handlers
# Short routines so the GUI does not become non-responsive.
# Command callbacks (command)
#  - avaiable for interactive widgets that product events
#  - configure by setting widget's command property
# Event bindings (mouse)
#  - used for events that do not have a command callback
#  - configure by using widgets "bind" method
