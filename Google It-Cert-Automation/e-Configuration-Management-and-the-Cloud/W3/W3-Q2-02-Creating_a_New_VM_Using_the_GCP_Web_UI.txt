[navigating to console.cloud.google.com,]
[cloud console for GCP.]
[first step is to create a project so that our VMs are associated to that project.]
[create new project]
[Project name: First Cloud Steps]
[enu entry that lets us create virtual machines.]
[Compute Engine menu, and select the VM instances entry.]
[pressing the Create button.]
[Create INSTANCE]
[calling this machine linux-instance.]
[Now it's time to select the region and zone.]
[if you're deploying a service, you should select something that's close to your users.]
[select the type of machine that we want to use.]
[general purpose, compute optimized, and memory optimized.]
[how much CPU and how much memory we want our VM to have.]
[long list of available operating systems to choose from.]
[we'll just keep the default values here.]
[The default access option allows you to access the instance remotely using SSH]
[the creation wizard lets us pre-configure some firewall rules.]
[we'll want to connect to a web server on this machine, so let's turn HTTP on.]
[let's click on the command line link.]
[Once it's done setting up, we can connect to it using SSH.]
[For example, we can get a text version of the weather in our current location by calling the curl command, which we can use to access web pages from the command line, and passing in wttr.in as the website.]
