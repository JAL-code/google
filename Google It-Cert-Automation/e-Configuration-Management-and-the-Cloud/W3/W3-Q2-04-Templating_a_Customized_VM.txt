[click on the machine's name to see all its details, and then we can click on the boot disk.]
[Let's click create image.]
[Create an image header]
[call our image "webserver-image".]
[We're now ready to create our instance template.]
[Instance templates on navigation menu.]
[To do that, we'll go to the instance template option, and then click create new instance template.]
[name our template web server- template.]
[change the boot disk to use the image we've created.]
[use the custom Image we've created.]
[enable HTTP access to the instances created with this template.]
[Let's go back to the VM instances entry, and then click on create instance.]
[We'll name our instance web server-1]
[Click on the server ip address to display Hello Cloud.]
[To interact with Google Cloud, we'll be using the gcloud command.]
[authenticate to the Google Cloud system ]
[say yes here.]
[follow the process here, and authenticate.]
[select one here.]
[initializing wizard lets us select the default region and zone.]
[As we called out earlier, when selecting where to run your services, you should go with the one that's closest to you.]
[For our example, we'll use it to create five additional VMs.]
